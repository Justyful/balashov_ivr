<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.physicsfinal">

    <application
        android:name=".PhysicsFINAL"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GraphViewActivity"></activity>
        <activity android:name=".GraphActivity" />
        <activity android:name=".CalculatorActivity" />
        <activity
            android:name=".EditArticleActivity"
            android:noHistory="true" />
        <activity
            android:name=".NewArticleActivity"
            android:noHistory="true" />
        <activity android:name=".HistoryActivity" />
        <activity
            android:name=".ArticleContentActivity"
            android:noHistory="true" />
        <activity android:name=".ArticlesListActivity" />
        <activity android:name=".SectionsListActivity" />
        <activity android:name=".PhysicsFINAL" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>